# Python runtime deps for placo_ik online profiler (ROS 2 Humble / Python 3.10).
#
# numpy must be 2.2.x: placo 0.9.20 -> pin 3.8.0 -> cmeel-boost 1.89.0 hard-pins
# numpy>=2.2,<2.3 on cp310. This is the same combo that works on the host
# (numpy 2.2.x + placo 0.9.20). Do NOT force numpy<2 here — it makes the placo
# dependency chain unresolvable. scipy/matplotlib stay unpinned so pip grabs the
# latest wheels, which support numpy 2.x.
numpy>=2.2,<2.3
scipy
matplotlib
PyYAML

# placo + its FULL native cmeel subtree, pinned to the exact combo proven on the
# host. These are prebuilt binary wheels whose .so files are linked by soname
# (e.g. libtinyxml2.so.10, liburdfdom_sensor.so.4.0). placo/pin declare only
# loose lower bounds (`>=4`, etc.), so a fresh resolve drifts to newer wheels
# with bumped sonames and `import placo` dies with
# "lib<x>.so.<n>: cannot open shared object file". Pin the whole subtree so the
# linked sonames always match. Regenerate from a known-good box with:
#   pip show placo pin libpinocchio coal libcoal eigenpy eiquadprog cmeel \
#     cmeel-boost cmeel-urdfdom cmeel-tinyxml2 cmeel-console-bridge cmeel-octomap \
#     cmeel-qhull cmeel-assimp cmeel-zlib rhoban-cmeel-jsoncpp meshcat ischedule
placo==0.9.20
pin==3.8.0
libpinocchio==3.8.0
coal==3.0.2
libcoal==3.0.2
eigenpy==3.12.0
eiquadprog==1.3.0
cmeel==0.60.1
cmeel-boost==1.89.0
cmeel-urdfdom==4.0.1
cmeel-tinyxml2==10.0.0
cmeel-console-bridge==1.0.2.3
cmeel-octomap==1.10.0
cmeel-qhull==8.0.2.1
cmeel-assimp==6.0.5
cmeel-zlib==1.3.2
rhoban-cmeel-jsoncpp==1.9.4.9
meshcat==0.3.2
ischedule==1.2.7
